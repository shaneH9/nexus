# Copy this file to .env for local development. Never commit real credentials.
SRA_NEXUS_ENV=development
SRA_NEXUS_LOG_LEVEL=INFO

# Add provider-specific credentials only when the corresponding adapter exists.
# SRA_NEXUS_NEWS_API_KEY=
# SRA_NEXUS_MARKET_DATA_API_KEY=
